# Start With Explicit Capture

Sharebook will begin with user-initiated Explicit Capture through share sheet, upload, paste, drag-and-drop, or manual entry rather than importing existing saves from closed platforms or scraping in the background. This keeps the MVP resilient across social apps, avoids brittle platform dependencies, and lets the product validate Save Intent inference before attempting deeper integrations.
